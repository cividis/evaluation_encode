include: "rules/prepare_data.smk"


rule all:
    input:
        expand(
            "data/rp_matrix_{suffix}.parquet",
            suffix=["tss", "gene"],
        ),
        expand(
            "data/embeddings_{suffix}",
            suffix=["tss", "gene"],
        ),
